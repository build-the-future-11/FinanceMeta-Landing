# Optional production member-platform handoff.
# Leave unset until the canonical FinanceMeta member app URL is verified.
# Requirements: public-DNS HTTPS only; localhost/.local/single-label hosts, raw IP literals,
# credentials, and URL fragments are rejected by both the client handoff guard and production verifier.
VITE_MEMBER_APP_URL=
