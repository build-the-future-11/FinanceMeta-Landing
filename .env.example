# Public member-platform handoff. Production uses the same value from .env.production.
# Requirements: exact canonical HTTPS login URL; localhost, credentials, hashes, and other origins are rejected.
VITE_MEMBER_APP_URL=https://finance4all-global-reach.vercel.app/login
